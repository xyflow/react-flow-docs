---
sidebar_position: 2
title: useReactFlow
hide_table_of_contents: true
---

import CodeViewer from '/src/components/CodeViewer';

This example illustrates how to use the `useReactFlow` hook. It comes with a lot of useful helpers like `setNodes`, `addNodes`, `getEdges`, `removeEdge`, `zoomIn`, `zoomOut`, `setCenter`, `fitBounds` and more.

<CodeViewer
  codePath="example-flows/UseReactFlowHook"
  additionalFiles={['Sidebar.js', 'index.css']}
/>
